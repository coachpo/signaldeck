# 架构说明

本文件记录当前实现。已接受的终态设计独立维护在 [`迭代目标`](迭代目标.md)，冻结版本以 [`STATUS.md`](../STATUS.md#冻结迭代目标) 为准；下面的模块和运行拓扑不表示目标已经实现。

## 系统边界

SignalDeck 是一个由浏览器前端、FastAPI backend、PostgreSQL 和外部 provider 组成的单用户系统。浏览器通过同源或配置的 API base URL 调用 backend；backend 负责 HTTP API、schema/manifest 处理、数据库访问、运行调度和外部 LLM、市场数据、HTTP、MCP provider 的调用。

当前部署与使用事实以 [`STATUS.md`](../STATUS.md) 为准。根目录组合栈是本地/演示拓扑；拆分制品只有 backend 和 frontend 两种镜像，scheduler 复用 backend 镜像并切换进程入口。拓扑示例见 [`docker/compose.production.example.yml`](../docker/compose.production.example.yml)。

外部依赖包括 PostgreSQL/pgvector、LLM provider、finance/data provider、被 Workflow Package 声明的 HTTP endpoint 和可选 MCP server。外部响应属于不可信输入，必须经过现有 parser、schema、runtime tool 和 redaction 边界。

## 组件与职责

### 前端

`frontend/` 是 React 19 + Vite 应用，使用 React Router、TanStack Query、shadcn/Radix primitives 和语义化 Tailwind token。`src/routes.ts` 声明 dashboard、Workflow Packages、Scheduled Tasks、Model Connections、Runs、Templates 和 Reports 路由。feature page 拥有领域文案、路由参数、hooks、mutation、toast、导航和校验；共享组件保持 presentational。

`src/lib/api-client.ts` 区分 `/api/v1` 扩展面和 `/api` platform 面，统一处理 JSON、文本、下载、token 重试和安全错误详情过滤。查询 key 由 `src/lib/query-keys.ts` 统一管理。

### FastAPI 入口与 API 层

[`backend/app/main.py`](../backend/app/main.py) 创建 FastAPI 应用、Logfire instrumentation、CORS、可选 `BearerTokenMiddleware`、错误 envelope、`/health` 和 `/ready`。`/ready` 通过 PostgreSQL `SELECT 1` 判断就绪；它不检查 scheduler 是否存活或 provider 是否可用。

`backend/app/api/platform_router.py` 将平台面挂载在 `/api`，包含 Workflow Packages、Schedules、Model Connections、Tools 和 Runs。`backend/app/api/router.py` 将静态扩展 API 挂载在 `/api/v1`；当前 finance 扩展提供 Templates 和 Reports 路由。

外部 JSON 使用 camelCase，backend 内部使用 snake_case；`CamelModel` 和 schema 层负责别名及请求校验。`ApiError` 和请求校验错误使用 `{code, message, details[]}`，错误详情在 backend 和前端读取边界都进行安全过滤；token middleware 的 401 单独返回 `{"detail":"Unauthorized"}`。

### 服务与运行时

`backend/app/services/` 采用服务与 repository 分层：路由调用 service，service 组织事务和运行规则，repository 提供持久化查询。`RunService` 组合 launch、snapshot、execution、rerun 和 read projection；`run_read_projection.py` 负责读取投影，`run_rerun.py` 从原运行快照准备重跑，`RunQueueService` 负责领取、lease、heartbeat 和失效处理。服务层同时包含 provider adapter；外部 I/O 不限于 extension 目录，例如 `model_gateway_openai.py` 和 `model_gateway_openai_responses.py` 实现 OpenAI-compatible Chat Completions 与 Responses 协议。

[`backend/app/workers/run_scheduler.py`](../backend/app/workers/run_scheduler.py) 每轮先处理失效 lease 和到期 schedule，再领取 queued run 并清理过期历史。`workflow_package_schedule_materializer.py` 负责自动 fire，`workflow_package_schedule_service.py` 负责计划 CRUD、preview 和 run-now；两者共用 recurrence、input rendering 和 launch 边界。

scheduler 通过 PostgreSQL advisory lock 保证同一数据库内只有一个 worker 主循环，`RUN_SCHEDULER_MAX_ACTIVE_RUNS` 控制线程槽位。repository 使用 `FOR UPDATE SKIP LOCKED` 按 `queued_at`、`id` 领取；默认 `package:<key>` execution scope 由 FIFO 条件和 partial unique index 保证串行。lease 过期时将 run 及正在执行的子记录标为 `failed`、未执行子记录标为 `skipped`，不会自动续跑或重新入队。

### 扩展组合根

`backend/app/extensions/registry.py` 静态声明 `INSTALLED_EXTENSIONS`。`Extension` 可以贡献 API router、server-declared tool、runtime executor、provider factory、runtime dependency surface 和 package-private MCP tool owner。当前固定扩展为：

- `signaldeck.finance`：Templates/Reports API、finance providers、market/news/sentiment/report runtime tools；
- `signaldeck.digital_oracle`：prediction market、SEC、macro/rates、crypto derivatives、CFTC 和 options 等 runtime tools，不增加 API 或导航路由。

扩展通过静态 Python wiring 注册，不使用动态 import discovery、运行时安装或 marketplace。

### 持久化层

`backend/app/models/` 定义 SQLAlchemy model，`repositories/` 提供数据访问，`db/` 负责 engine、`create_all`、seed、数据库验证和启动恢复。启动初始化另有独立 PostgreSQL advisory lock；它不会变更已有表结构。内置包 SQL 通过 upsert 重置同 key 的受管理 preset。具体表、字段责任和删除关系以 [`data-model.md`](data-model.md) 为准。

工作流包保存当前 manifest source、compiled plan、hash 和依赖面；包内 agent、output schema、capability profile、私有 MCP、HTTP operation 和 workflow graph 保持在包 artifact 中。launch 时复制 executable package snapshot 到 run-owned 表，运行与 rerun 从冻结定义执行。冻结的 Model Connection runtime profile 不包含 API key；agent 执行仍按 connection key 获取当前凭据，HTTP operation 和扩展工具按 package ownership 读取当前 secret binding。快照不冻结外部数据或已安装扩展的代码版本。

## 主要数据流

1. 前端提交 YAML 或 package 编辑内容到 `/api/workflow-packages`。
2. parser 先检查 YAML source safety 和字段形状，再执行 graph semantic validation；compiler 生成排序稳定、可哈希的 compiled plan；preflight 投影 validation、launch metadata 或 strict readiness 结果。
3. launch 验证输入、解析 Model Connection 和 extension/tool dependencies，创建 queued run、planned step/invocation rows 和 immutable package snapshot。
4. scheduler 领取 run，按 `plan.steps` 顺序执行；步内先并发执行 agent，再并发执行 HTTP operation，两个批次依次等待完成。每一步提交状态、输入来源、输出、token、错误和可选 trace/span id。queued run 可立即取消；running run 在 step boundary 协作停止，当前外部调用不会被 cancel 请求同步中断。
5. read projection 将数据库状态转换为 API 的 list/detail/provenance 结构；secret-bearing 字段在存储读取、导出、日志和诊断边界被排除或脱敏。
6. Scheduled Task 由 materializer 将符合 recurrence、timezone、overlap 和 misfire 规则的 fire 转换为普通 queued run；schedule 删除后，run 保留自己的 provenance。

## 依赖方向

依赖方向保持为：

```text
浏览器页面/feature hooks
        ↓
前端 API client 与类型
        ↓ HTTP
FastAPI routers / schemas / error boundary
        ↓
services / runtime orchestration
        ↓
repositories / SQLAlchemy models / db
        ↓
PostgreSQL
```

静态 extension registry 作为组合根向 API、runtime tool 和 provider 贡献能力；服务通过明确的 provider/tool 接口使用扩展，不反向让扩展依赖页面。parser/compiler 负责 package artifact 的边界，scheduler 负责队列与生命周期，不把执行细节放入 API router。前端 shared components 不拥有 route、API 或 domain state。

## 接口与安全边界

- `/health` 仅表示进程存活；`/ready` 还要求数据库可连接。
- 设置 `SIGNALDECK_API_TOKEN` 时，除 `/health`、`/ready` 和 `OPTIONS` 外的请求需要 `Authorization: Bearer <token>`；本地默认配置可以不设置 token。
- `AGENT_PLATFORM_ENCRYPTION_KEY` 加密 Model Connection API key 和 package secret。读取、导出、run detail、日志、诊断和 metadata 不返回原始 secret。
- Workflow Package schema 默认 closed；禁止 `additionalProperties`、`allowAdditionalProperties` 和 `patternProperties`。secret reference 只进入 HTTP request 字段。
- HTTP operation 默认仅允许 `GET`、`POST`，默认拒绝不安全 HTTP、私有网络和重定向，并限制超时、请求/响应大小；配置改变这些边界时必须按受影响风险验证。
- MCP runtime 默认关闭，stdio command 使用允许列表；MCP 的 env、headers、query 属于 secret-bearing runtime config。
- manifest hydration 和 export 去掉数据库内部引用与私有 MCP 的 `env`、`headers`、`query`；API envelope 和 run provenance 保留 `packageId` 等安全 identity。运行读取、导出和错误详情各有过滤边界，不能直接序列化 ORM 或内部 compiled plan。

## 本地与拆分部署模型

本地启动由 `start.sh` 调用根 `docker-compose.yml`：`db` 提供 PostgreSQL/pgvector，`app` 在单个容器内运行 Nginx、FastAPI 和 scheduler；宿主机默认只发布应用端口。

拆分部署使用 backend image、单独 scheduler role 和 frontend image。frontend Nginx 将同源 `/api` 请求代理到 backend；scheduler 不提供 HTTP 面，只从数据库领取并执行 queued run。多个 scheduler replica 可以通过 advisory lock 安全竞争，但当前个人本地部署不以高可用为目标。

## 质量属性与已知限制

- **可复现性**：运行使用 immutable package snapshot；compiler section 排序、canonical JSON hash 和 export redaction 必须确定。历史读取不依赖当前 Model Connection 或 secret binding 的存在，新的运行或 rerun 仍需要当前依赖通过 readiness。
- **可观察性**：Run、step、agent invocation、HTTP operation、queue/progress、retry/failure 和最终输出均有持久化 projection；可选 Logfire trace/span id 不应成为运行正确性的前提。
- **数据完整性**：启动用 `create_all`、preset seed 和 stale-run recovery，没有迁移框架；schema 变更需要重建数据库。运行保留默认关闭，设置 `SIGNALDECK_RUN_RETENTION_DAYS` 后仅清理终态旧运行。
- **依赖限制**：FastAPI 当前保持 `>=0.136.3,<0.137`，直到 Logfire 的 OpenTelemetry 依赖允许 `opentelemetry-sdk>=1.43` 且 instrumentation 达到 `>=0.64b0`；原因和解锁命令见 [`handover-deps-follow-up.md`](handover-deps-follow-up.md)。
- **并发配置限制**：`RUN_SCHEDULER_MAX_ACTIVE_PER_PACKAGE` 当前只声明并记录在启动日志中，未参与领取逻辑；改变它不会解除默认 package scope 的串行约束。
- **边界风险**：外部 provider、网络请求和 MCP server 的可用性不由 SignalDeck 保证；本地内网便利性不能转化为公网、生产容量或多用户安全承诺。

## 当前架构例外

### 与已接受目标的结构差距

下表记录冻结时尚未完成的替换边界，不作为保留旧路径的理由。目标细节与验收以 [`迭代目标`](迭代目标.md) 为唯一依据；实现验证后同步更新本表和相应当前架构章节。

| 当前结构与证据 | 尚未达到的目标边界 | 当前约束与退出验证 |
| --- | --- | --- |
| [`RunService`](../backend/app/services/run_service.py) 按 `plan.steps` 顺序执行，步内分别等待 agent 与 HTTP operation 的并发批次 | 按合并依赖图调度就绪节点，汇聚只等待自身依赖 | 保留已提交输出与现有输入校验；用无关慢分支和输入/条件推导边的执行用例验证替换。 |
| [`run_scheduler.py`](../backend/app/workers/run_scheduler.py) 与 [`run_queue_service.py`](../backend/app/services/run_queue_service.py) 领取数据库队列；失效 lease 将运行终止为失败 | 单一持久执行权威、调用级恢复和取消传播 | 当前领取归属校验仍有效；执行引擎比较及故障注入通过前，不宣称可以恢复已中断工作。 |
| [`extensions/registry.py`](../backend/app/extensions/registry.py) 导入业务扩展并静态组合工具、provider 和 API | Core 不导入插件业务代码，插件可独立部署升级 | 现有授权检查和工具身份校验继续生效；第三个非金融插件无需 Core 改动即可接入，且未启用插件故障不影响通用服务后退出。 |
| [`RunWorkflowPackageSnapshot`](../backend/app/models/run.py) 冻结包与非敏感模型配置，未冻结插件制品身份 | 固定执行制品、工具契约及运行绑定 | 保留不可变包快照和 secret 安全投影；发布新插件后旧运行仍绑定原制品、制品缺失明确失败后退出。 |
| [`backend/pyproject.toml`](../backend/pyproject.toml) 尚无 Pydantic AI、Temporal、Prefect 或 Hatchet 依赖 | Agent Runtime 与唯一持久执行引擎集成 | Pydantic AI 是目标方向，Temporal 是首选候选；按目标文档比较并验证后更新实际依赖与运行说明。 |

### 本地配置例外

本地模式允许使用默认开发 encryption key 和不配置 API token，以减少个人调试的启动成本；`production`、`prod` 和 `staging` runtime mode 会拒绝默认数据库 URL 或 placeholder encryption key，并记录未配置 API token 的警告。该例外仅适用于本地/开发边界，补偿控制是现有 secret redaction、HTTP/MCP 限制、单用户内网部署假设和适用质量检查；当部署公开或进入生产时必须重新评估并使用显式凭据与认证边界。配置拒绝行为与健康检查由 [`test_runtime_config_health.py`](../backend/tests/test_runtime_config_health.py) 覆盖。
