# SignalDeck 个人使用优化实施计划

执行状态追加：用户后续已授权并完成 PU-S1–S6 的24项主体任务。下文“未授权/未实施”描述保留原规划建立时点，不限制后续执行；当前结果见[共同验收记录](personal-use-verification.md)。

## 规划基线与范围

规划日期：2026-09-10。源码基线：`5a993684eb35f655d46cffbec2c0a3f73c55fd4e`。配套任务分解见 [个人使用优化 Sprint Backlog](personal-use-sprint-backlog.md)。本文件拥有建议范围、工作包、依赖和验收；Backlog 拥有任务记录与 Sprint 安排。

本轮用户要求在上轮功能评估基础上分析优化迭代并拆分多个 Sprint。**规划获授权；具体扩展是本轮推荐方案，未逐项确认为既定产品要求，也不表示已授权开发。** 以下任务均未实施，验收方法与证据均为未来要求。本轮仅新增这两份规划文件，不修改 STATUS、当前规范、历史 Sprint 或现有实例。

已读取的依据：

- [当前任务与2026-09-10对话](codex://threads/01a08880-6f63-78a2-bd58-260a62426feb)：个人私用、保持 STATUS 状态；提供真实自定义 provider 执行工作流，模型回复无需模拟；本轮要求多 Sprint 规划。
- [STATUS](../../STATUS.md)、[产品说明](../产品说明.md)、[架构说明](../架构说明.md)、[贡献指南](../../CONTRIBUTING.md)、[解耦方案](../工作流解耦方案.md)：当前范围和 A01–A18、D01–D06 继续有效，普通模式可完整编辑合法 JSON 输入，Finance 保持业务所有权。
- [2026-09-10评估](../../output/playwright/product-analysis/analysis.md)及[证据摘要](../../output/playwright/product-analysis/evidence-summary.json)：本机忽略目录中的研究附件，不随 Git 检出交付。本计划下表保留必要结论与仓库复核入口，不要求新开发者必须取得本机附件才能理解范围。
- 已读“简化 SignalDeck 操作逻辑”“解耦预置工作流与平台”“实施 SignalDeck 工作流解耦方案”“冻结 Temporal 选型并授权切换”四个会话。其已落地决定以当前规范为准，不按历史方案重新建设已完成功能。
- [原 Sprint 清单](sprint-backlog.md)、[交付](sprint-delivery.md)、[独立验收](sprint-verification.md)属于已完成的2026-09-08迭代。保留历史，不继承其中已过时的四场景名单、复杂输入必须转专家或真人参与门槛。

交付目标：让个人操作者能快速接入并诊断服务、得到可读可带走的结果、看懂模型用量、处理自动执行结果，并逐步积累可恢复输入和可比较的资料。推荐六个增量 Sprint：前四个完成日常使用主线，后两个改善持续资料工作；编号不构成全部串行前置。

约束：保持本地内网、单一操作者、MVP、Workflow Package 唯一定义根、Temporal 执行权威、独立插件和不可变快照。凭据、unknown 写效果、取消语义、闭合 schema、camelCase、旧包 hash 与历史绑定继续遵守现有合同。排除账号/RBAC、多租户、插件市场、Studio/Tryout、memory、fork、portfolio、simulation/backtest、引擎替换与生产治理。新增存储遵守 `create_all` 不升级旧表的事实；本计划不授予重建现有数据库的权限。

责任只按工程角色标识：前端、后端、Notes 插件、集成验证。具体人员均未分配，同一执行者可承担多个角色；不据此假定多人并行或独立验收团队。每包的集成验证角色为验收责任方，每个 Sprint 指定汇总任务承接。没有日期、固定周数或人日承诺；工作量只用小/中/较大表达相对复杂度。

## 需求来源与处理决定

`pu-r*` 是本计划对已读评估的局部引用，不改写产品验收编号。下表“推荐”是规划建议，“后置候选”不进入六个 Sprint 的完成范围。

| 引用 | 基线事实与来源 | 本轮规划决定 |
|---|---|---|
| pu-r01 接入与信息负担 | [task-connections](../../frontend/src/pages/platform/task-connections.tsx) 在可用预设未选中时错误显示无预设；[task-preparation](../../frontend/src/pages/platform/task-preparation.tsx) 普通模式展示完整技术配置 | 推荐先修预设状态，再折叠低频配置与原始对象；绑定变化、业务范围、失败/unknown 不得被隐藏 |
| pu-r02 正文排版 | [result-sections](../../frontend/src/pages/platform/result-sections.tsx) 缺少 Markdown 排版；动态 OL 清单编号消失 | 推荐统一声明正文、历史正文与附件排版，保持原文字节与状态 |
| pu-r03 可操作诊断 | [prepare API](../../backend/app/api/platform_packages.py) 仅补插件观察；[model_runtime](../../backend/app/infrastructure/model_runtime.py) 将 HTTP 错误归为通用码 | 推荐补模型最近调用身份/时间及安全错误类别；没有原始证据时不倒推历史具体原因 |
| pu-r04 结果交付 | [result-view](../../frontend/src/pages/platform/result-view.tsx) 有正文和附件入口，无统一正文复制/成品导出；Finance 已有报告下载 | 推荐复制和 Markdown 导出；PDF/打印后置；Core 不取得 Finance 模板/报告管理权 |
| pu-r05 模型用量与预算 | [Budget](../../backend/app/domain/definitions.py) 有总 token/调用限制；[model_settings](../../backend/app/infrastructure/temporal_agent.py) 将剩余总预算作为单次输出上限；模型证据有 usage | 推荐用量/耗时投影与独立单次输出上限；不建计费系统，不承诺费用预测，不把上轮配额拒绝认定为预算 bug |
| pu-r06 结果整理 | [历史查询](../../backend/app/infrastructure/run_history.py) 支持搜索筛选分页，任务已有收藏；结果缺少个人标记 | 推荐结果收藏、已读、个人备注；主题/标签与批量整理后置 |
| pu-r07 执行后处理 | 已有 Run 与 schedule/fire 状态，无用户通知能力；fire 可能启动失败而没有 Run | 推荐站内未读/待处理入口，包含无 Run 的 fire 失败；外部或浏览器通知渠道后置，不能默认新增外发 |
| pu-r08 可恢复输入 | [task-launch](../../frontend/src/pages/platform/task-launch.tsx) 草稿为内存 Map；刷新清空符合现行产品合同 | 推荐显式服务端草稿，与收藏/常用配置分开；这是新合同，不作为旧功能缺陷 |
| pu-r09 跨次比较 | 已有历史输入复用与 sourceRunId，没有结果比较界面 | 推荐固定两次 Run 的并排阅读与确定性文本差异；模型变化摘要后置 |
| pu-r10 Notes 积累 | [Notes](../../plugins/notes/notes_plugin/main.py) 只有不可变创建、集合内字面搜索，没有自有浏览页面 | 推荐 Notes 自有只读浏览、搜索、分页和公开结果链接；不加修改/删除不可变笔记或隐式记忆 |
| pu-r11 资料接入与新用途 | 当前主要粘贴文本；文件/URL、周报/网页变化等仅为研究建议 | 后置候选：先选一个真实来源和一个高频用途，再细化独立插件及包；不同时承诺所有格式 |
| pu-r12 专家制作 | [package-structure](../../frontend/src/pages/platform/package-structure.tsx) 的复杂编辑仍依赖 JSON，Graph 是查看器 | 后置候选：字段/映射辅助、节点添加复制和修订比较；保持同一 YAML |
| pu-r13 本地诊断与容量 | [health/ready](../../backend/app/main.py) 不证明执行栈完整可用；[数据模型](../data-model.md) 没有自动清理 | 后置候选：状态/占用只读可见性；清理需另外细化引用和数据影响，不纳入本轮优化收口 |
| pu-r14 更强自动化 | [Node/Workflow](../../backend/app/domain/definitions.py) 为固定 DAG，未提供动态展开或等待交互节点 | 后置候选：事件接入、有界批处理、本人补充输入；先确认重复需求，再演进合同 |

## 假设与尚缺条件

| 编号 | 类型与内容 | 影响与处理 |
|---|---|---|
| pu-o01 | 环境：上轮 `prism/deepseek-v4-flash` 先有成功调用，后续直接接口复核返回 `insufficient_user_quota`；当前额度与服务可用性未重新核实 | 不阻塞本轮规划或独立本地开发。真实模型验收需执行时核实接口、模型权限和可用额度，不能用模拟成功回复关闭；不要求充值，也不自动换账户 |
| pu-o02 | 协议：单次输出上限的字段位置/默认值尚未设计 | pu-budget-contract 必须先交付闭合字段、旧定义省略行为、两种 API 映射与旧 hash 验证。后续消费者依赖该子集，不等整个用量页面完成 |
| pu-o03 | 数据：附属元数据/草稿/站内处理标记需要持久化，尚无物理表设计 | 优先独立附属记录。契约任务说明新增表能否通过既有初始化落地；若必须改已有表，先形成具体数据转换路径，不假设 `create_all` 自动处理或原数据可丢弃 |
| pu-o04 | 业务：没有确认最常用文件/URL来源、外部通知渠道、精确费用要求 | 只将确定性的站内处理、Markdown 导出、既有文本 Notes 纳入主体；相应扩展保留在 Backlog 唯一未排期区，不成为所有任务的前置 |
| pu-o05 | 规划假设：Notes 是已有真实可验证的参考场景，尚未证明它是用户最高频业务 | 用 Notes 验证通用合同，另用无关包名/字段做解耦回归；不把 Notes 语义写入 Core，也不据此承诺新业务接入 |
| pu-o06 | 资源：人员、实际并行容量和时间未提供 | 按可用增量排序；每项具备输入不代表已派工。不安排会议、审批或真人观察作为前置 |

## 工作包

全部工作包验收归集成验证角色（人员未分配）。下表“协作/最终集成”不是整包启动前置；Backlog 指定具体任务和可用子集。

| 工作包与来源 | 结果、边界与必要产物 | 主责 / 协作与接收方 | 启动前置、接口与最终集成 | 验收 |
|---|---|---|---|---|
| pu-wp01 接入与阅读；r01/r02 | 连接五态（加载/失败/无候选/待选/已选），共享 Markdown，普通配置/回执/输入渐进披露；完整证据仍可达 | 前端 / 后端提供安全准备模型；结果交付/比较任务接收共享正文组件 | 连接与排版可独立开始；披露采用现行 sections/changedBindings，无新业务字段推断；最终与 wp02/wp03 联合 | pu-a01/02/04 |
| pu-wp02 模型诊断；r03 | 白名单错误分类和安全请求标识；模型最近观察的资源、配置身份和时间；准备/设置/结果/fire 页面消费 | 后端 / 前端；wp04/wp05 接收观察和错误类别 | 可从现有证据与适配器开始；只读投影不探测服务。接口子集就绪即可开发 UI；真实联合验证依赖 pu-o01 | pu-a03 |
| pu-wp03 成品交付；r04 | 复制确认正文、Markdown 文件和明确状态/来源；无正文/部分成功/大产物有可操作结果 | 前端 / 后端核对安全投影，Finance 验证现有下载不受影响；wp07 接收确认内容选择规则 | 正文选择先核对现行冻结 sections/产物；不新增业务识别器或模板系统。最终需 wp01 正文样式 | pu-a04 |
| pu-wp04 用量与预算；r05 | 逻辑模型调用去重的 token/耗时摘要、当地日汇总；缺值标记；独立输出上限的闭合合同、执行映射与编辑控件 | 后端 / 前端；wp02 的错误类别用于失败说明 | 用量读投影可独立开始；上限执行/编辑依赖 pu-o02 契约。最终需真实模型验证与旧快照回归，不能把 token 当账单 | pu-a05 |
| pu-wp05 结果管理与处理；r06/r07 | 独立收藏/已读/备注数据与 UI，站内处理项和状态变化身份；有 Run 与无 Run 的 fire 失败均可发现 | 后端 / 前端；wp06 可复用持久化冲突处理约定但不共用业务记录 | 附属元数据合同可先做；站内处理定义依赖结果/fire 身份及该可用子集；最终与 wp02 的原因及已提交状态联通。无外部投递 | pu-a06/07 |
| pu-wp06 服务端草稿；r08 | 显式保存/恢复/删除草稿；保留修订、JSON根值、未应用文本、冲突及待确认启动身份；不存凭据 | 后端 / 前端；现有任务、常用配置与历史复用共同接收 | pu-o03 数据方案与既有 LaunchInputs/启动身份即可开工；不依赖 wp07 比较。最终验证刷新、定义变化和响应丢失 | pu-a08 |
| pu-wp07 结果比较；r09 | 固定两次 Run 的确认内容、来源/修订/时间/状态，并排阅读和确定性差异；不作自动业务解读 | 前端 / 后端处理必要安全投影；接收 wp03 内容选择合同 | 确认内容选择规则可用即可开始，不依赖草稿或结果标记完成；最终需 wp01/wp03 及不同状态实际历史 | pu-a09 |
| pu-wp08 Notes 工作区；r10 | Notes 自有只读 HTTP/API与页面，集合/字面搜索/分页，发布 pageUrl/resultLink；新 Run 可直达，旧 Run 保持原冻结身份 | Notes 插件 / 前端与后端仅对通用链接合同做核验 | Notes 现有数据和独立运行库即可开始；发布链接契约在页面前可先稳定。最终需实际 Notes、新发布 Run 和离线 Core 历史 | pu-a10 |
| pu-wp09 分阶段联合验证；r01–r10 | 每个 Sprint 都有完整业务增量和适用回归；最终共同验收、当前规范与机器合同同步 | 集成验证 / 各产物责任角色交付可复现入口和版本 | 测试边界准备可先做；各 Sprint 汇总只依赖其实际贡献产物，最终收口依赖主体工作包已验证输出。真实模型条件缺失时保留该义务 | pu-a11，并汇总 a01–a10 |

## 验收与共同结果

下列 `pu-a*` 是本计划的局部验收，不是已通过记录或 GOAL 用例。全部条目都要求关联将来实际源码版本、运行命令、结果和必要截图/调用身份。既有 A/D 合同只按变更影响复验，不机械给无关工作追加全部验收。

验证条件统一引用：**L**＝按 CONTRIBUTING 的隔离 PostgreSQL、Temporal、dispatcher、固定制品 Worker、真实 Notes/MCP 与前端构建；**B**＝既有 Vitest/Playwright，相关页面375/768/1024/1440宽度及键盘操作；**M**＝用户授权的真实接口、模型和可用额度，使用无敏感测试资料，回复不模拟；**H**＝可复现的成功/失败/取消/unknown/无声明/大产物历史样本。H 中固定错误样本用于确定性回归，不替代 M 的真实成功证明。验证责任均为集成验证角色。

| 验收 | 可观察结果与贡献产物 | 方法与必要条件 | 预期证据 / 关联现行合同 |
|---|---|---|---|
| pu-a01 | wp01：有候选时正确提示选择；无候选/加载失败有不同路径；原输入与绑定核对不丢 | 组件覆盖五态，L+B 实际补齐 Notes 连接并执行；核对变更连接与稳定 launch 身份 | 状态截图、原参数与 Run/回执；普通任务连接合同、A12/A14、D03 |
| pu-a02 | wp01：三个正文入口的标题/段落/清单/表格/代码清晰，窄屏可读，纯文本字节未被改写 | B 渲染样本与真实确认正文，检查列表标记和长文本/表格滚动 | 截图、内容对照、聚焦组件/E2E结果；D04/D06 |
| pu-a03 | wp02：已知额度/认证/限流/模型不存在/输入过长与未知错误可区分；最近观察对应实际绑定且附时间；错误不外泄 secret | 安全错误分类回归、资源替换后的观察隔离、L+M 实际成功/工具调用；已有真实配额复核只作历史样本，不补写原400详情 | 安全错误样例、模型/attempt关联、准备/结果/fire截图；A13/A14/A15、D06 |
| pu-a04 | wp01/wp03：当前结果可复制/导出，部分产物注明状态；失败模型文本不提升为成品；unknown/取消请求含义保留；Finance 原下载可用 | L+B+H；插件/引擎关闭后从 Core 已确认内容导出，核对来源、真实数据时间与缺省/大产物 | 文件内容与冻结输出对照、下载/剪贴板失败路径、离线记录；A08/A09/A10/A15、D04/D06 |
| pu-a05 | wp04：每次与当地日 token/耗时可核对；缺 usage 不计为0，恢复/重试不重复计确认值；新上限生效，旧定义 hash/预算语义不漂移 | 后端聚合与边界测试、L+M 真实模型记录，chat_completions/responses 两适配映射回归；费用不作账单，未确认网络消费显示覆盖限制 | 用量对账、上限请求证据、旧hash/快照/新Run对照；A03/A07/A10/A11/A14 |
| pu-a06 | wp05：收藏/已读/备注跨刷新保留，更新不改冻结Run或输出；取消收藏/删除备注不删除结果；未读变化不掩盖真实unknown | L+B；不同窗口修改冲突、历史筛选分页/snapshotAt、不同当地日期、字段保真 | 附属记录与原Run对照、历史导航/刷新记录；A10/A15、历史查询合同 |
| pu-a07 | wp05/wp02：完成、失败、unknown变化及无Run的fire失败进入站内处理；重复观察不重复生成同一变化，已读不改变实际状态 | L+B+H；关闭浏览器后运行完成再打开；重复状态投影/重启/unknown核实；L+M至少一次自然定时成功，失败路径另核对 | schedule/fire/run与处理项身份、无Run失败入口、暂停保持、轮询无执行副作用；A08/A09/A12/A17 |
| pu-a08 | wp06：草稿恢复保留原修订、null/缺失/空值/数组及未应用JSON；定义变化不补默认值；冲突不无声覆盖；不因恢复而重复启动 | L+B；刷新与跨窗口、修改定义、同一请求响应丢失、旧Run复用；未应用JSON仍须显式处理，凭据不进入草稿 | 保存前后值、冲突/重校验界面、稳定启动身份与独立Run记录；A10/A12/A14、D03及草稿新合同 |
| pu-a09 | wp07/wp03：比较固定的两个Run，明确两边状态/来源/修订/时间，文本差异不被称为业务变化；无正文/大产物有明确路径 | L+B+H；历史无声明、不同字段/修订、局部结果、插件离线，以及相同正文不同执行身份 | 固定比较链接/选择、差异截图、两侧内容与原输出对照；A10/A15、D04/D06 |
| pu-a10 | wp08：Notes 页面可找回真实保存笔记；浏览/搜索/分页不改笔记；新链接由公开发布冻结，旧Run不被重绑；Notes离线不阻断Core历史 | 实际 Notes HTTP/MCP+PostgreSQL、B、新旧发布/新旧Run和离线；核对插件不读Core私有表；必要真实笔记链用M | 笔记记录、页面操作、发布/operation/Run链接与离线证据；A01/A02/A10/A14、D01/D05/D06 |
| pu-a11 | wp09：从准备、执行、阅读/导出、标记/处理、草稿/比较到Notes页面形成一致闭环，适用规范同步 | 各阶段验证不后移；最终按受影响范围运行必需门禁与 L+B+H+M。无关包名/Workflow/字段进行通用性验证；不要求真人参与 | 六阶段证据索引与最终共同结果，实际版本/命令/未验条件；受影响A01–A18、D01–D06与普通操作合同 |

真实模型成功验收至少保留“内置笔记整理”“模型主动工具调用”“自然定时成功”三条路径。可在最终同一实现版本上复用共享证据，不能用三条不同且未核对的旧版本代替。额度不足只阻塞涉及 M 的完成结论；不阻塞排版、只读投影、存储和固定错误回归，也不允许用 mock 成功冒充已完成 M。

各功能交付时同步受影响的唯一权威：行为到产品说明，结构到架构/数据模型，闭合协议及示例到对应合同，视觉到 DESIGN，插件到独立扩展文档。本轮不先把规划写成当前事实；不要求每个任务都修改所有文档。检查使用项目现有入口；不创建新的校验脚本、GOAL、任务派发或治理流程。

## 覆盖与后续边界

| 来源 | 工作包与验收 | 覆盖安排 |
|---|---|---|
| r01/r02 | wp01；a01/a02/a04 | Sprint 1修正，Sprint 2完成披露；两阶段都有联合验证 |
| r03 | wp02；a03 | Sprint 1，真实模型成功条件跨阶段保留 |
| r04 | wp03；a04 | Sprint 2；PDF/打印仍在未排期区 |
| r05 | wp04；a05 | Sprint 3；主动探测/模型发现/精确费用后置 |
| r06/r07 | wp05；a06/a07 | Sprint 4；站内处理先交付，外部通知/主题整理后置 |
| r08/r09 | wp06/wp07；a08/a09 | Sprint 5；两包可独立，模型变化摘要后置 |
| r10 | wp08；a10 | Sprint 6；只读库与已有文本工作流，不含新增文件采集 |
| r01–r10共同结果 | wp09；a11 | 每Sprint汇总任务和Sprint 6最终共同验收 |
| r11/r12/r13/r14及上述扩展项 | 尚未分配主体工作包 | Backlog未排期区保留目的、验收义务和细化条件；不计入主体完成范围，不伪造已接受方案 |

建议先以 Sprint 1–4 作为能独立产生主要收益的一组交付。Sprint 5–6 是下一组推荐增量，可按实际使用频率调整顺序。任何调整都保留任务ID和依赖的具体产物，不通过重排弱化真实验证或解耦边界。
