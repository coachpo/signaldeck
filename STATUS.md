# 项目状态

开发档位：MVP

## 生命周期

SignalDeck 当前以本地开发调试和核心产品闭环验证为交付阶段。开发档位选择 [`CONTRIBUTING.md`](CONTRIBUTING.md#当前开发策略) 的静态执行默认值；核心流程与验收由 [`产品说明`](docs/产品说明.md) 定义，不构成对生产运行的承诺。

## 冻结迭代目标

已接受 [`迭代目标`](docs/迭代目标.md) 作为后续迭代的目标基线，标识为 **SD-TARGET-001**。来源是会话「设计 Agent 平台理想架构」的完整结论及最后修正；目标正文、设计边界、选型状态和目标验收仅在该文档维护。当前已实现行为仍由 [`产品说明`](docs/产品说明.md)、[`架构说明`](docs/架构说明.md) 和源码记录，不能把目标已接受等同于实现已完成。

冻结版本是首次引入 `docs/迭代目标.md` 的文档包提交 `bca05dcd666e96561426224b89b0e06aed186a22`；该提交中的根文档、`docs/` 文档及适用的 `AGENTS.md` 共同构成基线。后续迭代引用该提交的完整 SHA 与目标/验收编号，不使用移动中的分支名代替冻结版本。在保留完整历史的仓库中可核对该 SHA：

```bash
git log --diff-filter=A --format=%H -- docs/迭代目标.md
```

后续实现与验收以冻结提交及用户明确作出的补充决定为目标参照，当前文档随已验证实现更新并说明剩余差距。改变已接受目标需要新的明确决定，不能通过修改当前实现说明或引用会话的新内容默默替换基线。

2026-09-08，用户明确确定使用 Temporal。该补充决定已写入 [`迭代目标 T06.2`](docs/迭代目标.md#t062已确定使用-temporal)，覆盖原冻结版本中“执行引擎尚未定案”的状态；原始冻结 SHA 和 T01–T08、A01–A18 编号保持不变。后续任务以原冻结提交加本次 Temporal 选型决定执行，保留三候选比较证据，并继续完成 Temporal 集成及全部必需验收。引擎定案不代表目标实现或验收已完成。

## 部署与使用

当前部署边界是本地内网，使用对象是个人和单一操作者。项目优先保持本地启动、调试、观察和日常使用便利；这项偏好不取消现有的正确性、数据完整性、密钥保护和必要验证边界。

仓库提供本地组合栈和拆分部署示例，但这些文件不能证明实际实例的部署、外部用户或数据状态；当前未核实“没有外部用户”或“没有不可丢弃数据”。MVP 档位不替代这些事实，也不授予数据重置权限。

## 数据与兼容性

Core 配置和运行证据由 PostgreSQL 持久化，Temporal 保留执行历史，内容寻址目录保存产物与固定 Core 制品。Finance 和 Notes 使用各自数据库及角色。数据库启动通过 SQLAlchemy `create_all` 和缺失示例包种子完成；执行恢复由 Temporal 驱动。仓库没有 Alembic 或旧数据自动迁移路径，`create_all` 不会升级已有表。当前没有运行历史自动清理入口；数据结构和不可变边界见 [`数据模型`](docs/data-model.md)。

当前没有单独声明的长期外部 API、配置或数据库 schema 兼容承诺；修改仍须遵守产品范围、架构边界和适用质量检查，不能因为本地便利而泄露 secret、破坏运行快照或绕过数据完整性校验。

SD-TARGET-001 从理想终态设计，不以历史实现、迁移成本或任何兼容性选项约束目标，也不要求为旧 API、YAML、数据结构或插件实现保留兼容路径。目标设计本身不授予数据操作权限，实际处置按以下用户授权执行，并明确具体数据影响。

2026-09-08，用户授权现有实例在实际切换新实现时的数据处置。届时根据具体实例和数据情况确定保留、迁移或重建方式；当前继续隔离开发与测试，不因此立即变更现有实例数据。这项授权仅覆盖本项目切换所需的实例和数据，不表示现有数据已被核实为可丢弃，也不扩展到无关实例或数据。

## 允许与禁止的变更

允许在当前本地内网边界内改进开发启动、调试观察、工作流编辑和个人使用路径，并复用已有组件和依赖。涉及本地数据库重建、数据删除或兼容行为变化时，仍需在变更范围内明确影响并通过适用验证。

SD-TARGET-001 的本地实施已将旧静态扩展与 scheduler 替换为独立插件、Temporal DAG/Agent 执行、命令 dispatcher 和固定制品 Worker。2026-09-08 完成本地 A01–A18 验收：504 项后端测试、169 项前端单元测试、13 项浏览器回归及必需静态检查通过；实际 Compose 验证了定义/资源编辑、手动与定时运行、Finance 业务页面、调用证据和产物读取，并验证当前固定制品的 Worker 硬中断恢复及插件/核心独立升级。执行角色全部停止后，历史证据与产物仍可读取。当前产品和架构文档记录这些已验证边界；三候选比较的覆盖差异见 [`执行引擎比较`](docs/执行引擎比较.md)。

本次验证使用隔离源码副本、本地测试模型和专用数据库，未切换现有实例或处置其数据，不构成生产部署验证。实施任务已获本地实施、所需依赖、三引擎比较及隔离测试资源创建/清理授权；提交、推送、发布、部署及其他外部写入仍须遵守相应明确授权。

不得把根目录本地/演示组合镜像当作受支持的生产制品；不得把单用户产品扩展为认证/RBAC、多租户、插件市场或其他未重划范围的产品面；不得移除 secret 加密与脱敏、工作流包解析安全、确定性编译、运行不可变快照或仓库必需检查。

如果部署转为公网或生产环境，或确认存在外部用户、真实/不可丢弃数据、明确的兼容承诺和安全验收，必须重新评估本状态、开发档位及相关架构边界。
