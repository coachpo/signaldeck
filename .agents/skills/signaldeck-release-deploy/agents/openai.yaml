interface:
  display_name: "SignalDeck Release Deploy"
  short_description: "Release and roll out SignalDeck with staged gates"
  default_prompt: "Use $signaldeck-release-deploy to plan a gated SignalDeck release and rollout from an immutable image manifest."
policy:
  allow_implicit_invocation: true
